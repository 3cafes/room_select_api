const HOST = 'localhost';
const PORT = 27017;
const DATABASE = 'room_select_db';

//be careful with theses data
//you better to import them from a file ignored by git
const USERNAME = 'root';
const PASSWORD = 'root';

module.exports = {
	HOST,
	PORT,
	DATABASE,
	USERNAME,
	PASSWORD,
};
